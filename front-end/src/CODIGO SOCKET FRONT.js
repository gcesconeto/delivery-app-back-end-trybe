// const ioClient = require('socket.io-client');

// const port = 3001;

// const socket = ioClient.connect(`http://localhost:${port}`, {
//     reconnect: true,
// });

// socket.on('statusUpdate', ({ saleid, newStatus }) => FUNÇÃO_UPDATE(saleid, newStatus));
